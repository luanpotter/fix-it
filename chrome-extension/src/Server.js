FixIt = FixIt || {};

FixIt.Server = (function () {

  var server = 'http://fixit-server.appspot.com/api/fix';

  return {
    list : function (url) {
      var domain = URI(url).hostname();
      //yawp();
    }
  };

}());
